# 贡献与维护

欢迎把 BA仔作为一个可持续维护的本地开源项目来改进。提交前请阅读 `docs/AGENT_HANDOFF.md`；它同样适用于人类开发者。

## 基本流程

1. 用一个清楚、可回退的小改动解决一个问题。
2. 不提交 API 密钥、用户数据库、聊天记录或系统路径中的私密文件。
3. 对新功能补充对应文档；对会改变用户数据的功能说明迁移和恢复方式。
4. 运行 `npm run verify`，并在涉及透明窗口时补做 Windows 手工检查。

## 适合优先贡献的方向

- 动作状态机与低打扰的自主行为。
- 托盘控制、开机启动、备份与恢复。
- AI 工具调用的“预览—确认—写入”流程。
- 更宽容的 Excel 导入和错误提示。
